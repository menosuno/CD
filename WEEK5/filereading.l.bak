%option noyywrap 
%{ 
#include<stdio.h> 
%} 
%% 
 
%% 
main(int argc, char* argv[]) 
{ 
	 if(argc > 1) 
	 { 
		char fp[] = "inp.txt";
	 	FILE *fp = fopen(inp.txt, "r"); 
 	 	if(fp) 
			yyin = fp; 
	 	} 
	 yylex(); 
	 return 1; 
}
